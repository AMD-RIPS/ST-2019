### Archived individual Models

These are the models that we have built when we construct the ensemble model. We have decided to archive these models because these models are beaten in terms of accuracy. 
